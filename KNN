KNN
K-NearestNeighbor，K-最近邻算法
前K个最近样本（最近的K个邻居）中选择占比最高的类别作为预测类别

理论基础
1.计算逻辑：
(1)计算 测试对象 与 训练集中每个对象 的距离
(2)圈定距离最近的K个训练对象，作为测试对象的邻居
(3)根据这K个训练对象（邻近对象）的所属类别，找到占比最高的类别作为 预测对象的预测类别

2.影响算法准确度的因素：
1) 距离度量：常见的距离公式
(1)曼哈顿距离（Manhattan distance）:|x1-x2|+|y1-y2|
(2)欧式距离（Euclidean Metric）：√(〖(x_1-x_2)〗^2+〖(y_1-y_2)〗^2 )

2) K值选择（超参数调优）

3.
